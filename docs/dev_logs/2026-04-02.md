# 2026-04-02

## 会话概览

- 当前分支：`codex/cyd-stumpel-home-exploration`
- 当前远端跟踪分支：`origin/codex/cyd-stumpel-home-exploration`
- 本次目标：吸收“外部专家修订”的结果，用老杨协议把当前开发进度保存清楚，并转成一个新窗口可直接接手的部署运维交接口径

## 本轮新增确认

### 1. 外部专家给出的新交接方向

专家交接的主线已经从“继续诊断根路径问题”切到：

1. 代码修改已完成
2. 当前主要任务变成：
   - 将修改同步到 `WordPress-L1ta`
   - 触发 Railway 重部署
   - 做部署后验收

专家要求部署运维同学按以下顺序处理：

1. 同步 MU Plugin 到 Railway WordPress-L1ta
2. 触发 Railway 重部署
3. 验收根路径跳转与公开接口
4. 暂缓处理前台 `WORDPRESS_API_URL` 切换与管理员密码轮换

### 2. 已对照代码现场的实际确认

本地工作树中，专家提到的三类改动都已经能在代码里看到：

1. 管理域名根路径跳转
   - 文件：`wordpress/mu-plugins/mindhikers-cms-core/bootstrap.php`
   - 已存在：
     - `add_action('template_redirect', [$this, 'redirectManageDomainRoot']);`
     - `redirectManageDomainRoot()` 方法
2. 博客 HTML 渲染 sanitize
   - 文件：`src/components/blog/post-content.tsx`
   - 已存在：
     - `sanitizeWordPressHtml(post.contentHtml)`
3. Revalidate secret 传递收紧
   - 文件：`src/app/api/revalidate/route.ts`
   - 已确认：
     - 不再从 query param 读取 `secret`
     - 仅接受 `x-revalidate-secret` header
     - 或 POST body 中的 `secret`

### 3. 一个必须向下一窗口说明的风险

专家交接原文写的是：

1. “本轮代码已完成以下三项改动，均已在本地仓库提交完毕，尚未部署到线上”

但当前这份本地工作树的实际状态是：

1. `git status --short --branch` 仍显示大量未提交修改与未跟踪文件
2. 当前 `HEAD` 仍停在：
   - `4d1fe02 refs MIN-23 MIN-24 MIN-29 close homepage hardening and phase cleanup`
3. 因此，从当前窗口能确认的是：
   - 代码已经在当前工作树中出现
   - 但不能仅凭当前 `git log` 断定这些改动一定已经作为新 commit 落在当前分支 HEAD 上

对下一窗口的要求：

1. 在做线上部署前，必须先核对“专家所说的本地 commit”是否已经真正落在当前分支可见历史中
2. 如果没有，应先确认：
   - 是未提交
   - 还是在其他 worktree / 其他本地 clone / 其他分支

## 当前最值得接手的新窗口工作

### 工作 A：先核对提交状态

目标：

1. 不带着错误的提交假设去做部署

动作：

1. 读取当前分支最新提交
2. 对照以下 3 个文件的变更是否已经在 commit 中：
   - `wordpress/mu-plugins/mindhikers-cms-core/bootstrap.php`
   - `src/components/blog/post-content.tsx`
   - `src/app/api/revalidate/route.ts`
3. 若专家所说 commit 不在当前分支，先定位：
   - 其他分支
   - 其他工作树
   - 或未提交状态

### 工作 B：若提交状态无误，再做部署

部署对象：

1. Railway 服务：`WordPress-L1ta`

部署文件：

1. `wordpress/mu-plugins/mindhikers-cms-core.php`
2. `wordpress/mu-plugins/mindhikers-cms-core/bootstrap.php`
3. `wordpress/mu-plugins/mindhikers-cms-core/README.md`

说明：

1. 这是本轮真正影响线上 WordPress 的文件集合
2. 其中根路径跳转逻辑只在 MU Plugin 中

### 工作 C：部署后验收

最小验收命令：

```bash
curl -I https://homepage-manage.mindhikers.com/
curl -s https://homepage-manage.mindhikers.com/wp-json/mindhikers/v1/homepage/zh | head -c 100
```

预期：

1. 根路径 `/`
   - 返回 `302`
   - 跳到 `/wp-admin/`
2. `wp-json/mindhikers/v1/homepage/zh`
   - 继续返回 `200`
   - 继续可读

### 工作 D：如出现异常，按专家口径回滚

回滚点：

1. 删除 `redirectManageDomainRoot()` 方法
2. 删除 `registerHooks()` 中的：
   - `add_action('template_redirect', [$this, 'redirectManageDomainRoot']);`
3. 重新同步 MU Plugin 并触发 Railway 重部署

## 给新窗口的明确问题定义

请不要把下一窗口的任务表述成：

1. “继续排查为什么首页 CMS 不通”
2. “继续排查 Cloudflare Access”
3. “继续排查 WordPress 为什么还认临时域名”

这些问题在当前窗口里都已经被排除或收口。

新窗口应把问题精确表述为：

1. 专家已经给出一组待部署代码改动
2. 当前首要任务是核对这组改动是否真的已提交到当前分支可见历史
3. 若已提交，则执行部署到 `WordPress-L1ta`
4. 部署后验证：
   - 根路径 `/` 是否跳到 `/wp-admin/`
   - 已生效的 Cloudflare Access 是否保持不变
   - 公开 `wp-json` 接口是否继续正常

## 给新窗口的开场提示建议

可直接复制：

```text
老杨，继续接手 Mindhikers Homepage CMS 的部署运维收口。当前目录是 /Users/luzhoua/Mindhikers/Mindhikers-Homepage，分支是 codex/cyd-stumpel-home-exploration，upstream 是 origin/codex/cyd-stumpel-home-exploration。先读 docs/dev_logs/HANDOFF.md 和 docs/dev_logs/2026-04-02.md。不要重复做 DNS / Access / WP_HOME / WP_SITEURL 诊断。当前唯一任务是：核对专家所说的根路径跳转、博客 HTML sanitize、revalidate secret 收紧这三组改动是否已经进入当前分支 commit；如果已进入，就把 MU Plugin 文件同步到 WordPress-L1ta，触发 Railway redeploy，并验收 homepage-manage.mindhikers.com 根路径是否 302 到 /wp-admin/，同时确认 wp-json/mindhikers/v1/homepage/zh 仍返回 200。如异常，只按专家口径回滚 redirectManageDomainRoot() 相关逻辑。
```

## 当前不应在下一窗口继续扩大的范围

本轮暂不继续：

1. 前台 `WORDPRESS_API_URL` 切正式域名
2. 轮换 WordPress 管理员临时密码
3. 继续尝试 Cloudflare Redirect / Rulesets

原因：

1. 当前主线已被专家收敛为“先部署并验证根路径跳转”
2. 不应把部署窗口再次扩大为平台治理窗口

## 推荐新窗口第一阅读顺序

1. `docs/dev_logs/HANDOFF.md`
2. `docs/dev_logs/2026-04-02.md`
3. `docs/plans/2026-04-02_Homepage_Manage_Root_Redirect_Diagnosis.md`
4. `docs/plans/2026-04-01_Mindhikers_Cloudflare_Access_Execution_Checklist.md`

## 13:13 新增收口结果

### 1. 根路径跳转已完成上线验收

已确认：

1. `WordPress-L1ta` 的 MU Plugin 已按手工同步方式更新到线上
2. 已触发 `WordPress-L1ta` redeploy
   - deployment id: `e3b7c85a-dd1f-48b3-a2dd-f150f6c665ed`
3. `curl -I https://homepage-manage.mindhikers.com/`
   - 当前返回 `302`
   - `Location` 为：
     - `https://homepage-manage.mindhikers.com/wp-admin/`
4. `curl -I https://homepage-manage.mindhikers.com/wp-admin/`
   - 继续返回 Cloudflare Access 登录跳转
5. `curl -s https://homepage-manage.mindhikers.com/wp-json/mindhikers/v1/homepage/zh`
   - 继续正常返回 JSON

结论：

1. `homepage-manage` 作为 CMS 管理域名的根路径收口已完成
2. Access 没被误伤
3. 公开 Homepage JSON 接口没被误伤

### 2. 本窗口中途出现过一次远端文件事故，但已恢复

事故事实：

1. 一条错误的上传命令曾把远端
   - `/var/www/html/wp-content/mu-plugins/mindhikers-cms-core/bootstrap.php`
   写成空文件

处置方式：

1. 立即停止继续 redeploy / 扩大施工面
2. 改用整文件重写方式，把仓库当前版本重新写回远端 volume
3. 随后再次执行：
   - 远端 `php -l`
   - `sed` 抽样核对
   - HTTP 线上验收

结论：

1. 当前线上状态已恢复健康
2. 最终验收结果以本节记录为准，不再以前面的失败中间态作为判断依据

### 3. 主线已从“部署运维收口”切到“CMS 接管运营”

下一阶段建议：

1. 直接进入 WordPress 后台，确认 `mh_homepage` 的 `zh` / `en` 内容是否满足运营要求
2. 单独规划前台 `Mindhikers-Homepage` 服务切到 CMS 正式读取的上线窗口
3. 单独处理：
   - `src/components/blog/post-content.tsx`
   - `src/app/api/revalidate/route.ts`
   的发布口径
4. 等 CMS 运维链路稳定后，再轮换管理员临时密码

## 17:08 新增 Linear 同步与下阶段承接

### 1. 已按当前阶段收口 Linear issue

已处理：

1. `MIN-23` 已转为 `Done`
2. `MIN-24` 已转为 `Done`
3. `MIN-29` 已转为 `Done`
4. `MIN-7` 已转为 `In Progress`
5. `MIN-8` 已转为 `In Progress`

说明：

1. `MIN-23` / `MIN-24` / `MIN-29` 对应的页面框架、内容框架、UI 基础探索已经完成到足以收口的程度
2. `MIN-7` / `MIN-8` 仍保留为进行中，因为网站开发与上线主线并未结束，只是从“部署修复”切到了“运营接管与正式发布准备”

### 2. 已创建下一阶段承接条目

新建：

1. `MIN-110`
2. 标题：`网站运营接管：CMS 真实内容灌装与正式域名发布`
3. 状态：`Todo`
4. 归属：作为 `MIN-8` 的下一阶段承接 issue

这个条目承接的真实工作包括：

1. 用 CMS 开始灌 Homepage 真实内容
2. 清理当前 `wp-json/mindhikers/v1/homepage/zh` 中已观察到的少量乱码字符
3. 规划前台 `Mindhikers-Homepage` 服务切到 CMS 正式读取的上线窗口
4. 为主页正式发布配置真实域名
5. 准备 smoke 检查与回滚口径

### 3. 下一窗口不应再回到“部署排障”

新窗口应直接从以下问题定义开始：

1. `homepage-manage` 管理入口已可用，且根路径跳转、Cloudflare Access、公开 JSON 接口均已验收
2. 当前主线不是继续排查 Access / DNS / WordPress 真身域名
3. 当前主线是：
   - 通过 CMS 接管内容运营
   - 清理内容数据质量问题
   - 准备主页正式发布的真实域名与上线窗口

### 4. 推荐新窗口开场提示

```text
老杨，继续接手 Mindhikers Homepage 的 CMS 运营接管与正式发布准备。当前目录是 /Users/luzhoua/Mindhikers/Mindhikers-Homepage，分支是 codex/cyd-stumpel-home-exploration。先读 docs/dev_logs/HANDOFF.md 和 docs/dev_logs/2026-04-02.md。不要再回头做 DNS / Cloudflare Access / WordPress 真身域名 / 根路径跳转部署排查。当前已确认 homepage-manage.mindhikers.com 根路径会 302 到 /wp-admin/，wp-admin 会进入 Cloudflare Access，wp-json/mindhikers/v1/homepage/zh 继续返回 200。Linear 里 MIN-23 / MIN-24 / MIN-29 已收口，MIN-8 仍在推进，下一阶段承接条目是 MIN-110。当前唯一主线是：通过 CMS 灌真实内容、清理 JSON 中的乱码、规划前台切到 CMS 正式读取，并准备主页真实域名发布与回滚方案。
```

## 18:05 MIN-110 运营接管执行稿（CMS 灌装 + 正式域名发布）

### 1. 本窗口新增产物

1. 已新增执行文档：
   - `docs/plans/2026-04-02_MIN-110_CMS_Operate_And_Homepage_Domain_Release_Plan.md`
2. 文档已覆盖：
   - CMS `zh` / `en` 内容灌装清单
   - 前台切 CMS 正式读取的前置条件与动作
   - 主页正式域名发布 smoke 清单
   - 分级回滚口径（内容回滚 / 读取链路回滚 / 发布冻结）

### 2. 本次口径收敛

1. 主线维持 `MIN-110`，不回头做 DNS / Access / 根路径跳转排障
2. 域名角色固定：
   - 对外主页：`https://www.mindhikers.com`
   - CMS 管理：`https://homepage-manage.mindhikers.com`
3. 前台切换核心变量：
   - `WORDPRESS_API_URL=https://homepage-manage.mindhikers.com`

### 3. 下一步执行顺序（可直接照单推进）

1. 在 CMS 后台完成 `homepage-zh` / `homepage-en` 全量内容灌装
2. 逐项清理并复核接口返回中的潜在乱码字符
3. 在发布窗口执行前台 `WORDPRESS_API_URL` 切换
4. 执行 smoke 清单并记录结果
5. 如异常按 Level 1 -> Level 2 -> Level 3 回滚
