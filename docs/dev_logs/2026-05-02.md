# 2026-05-02

## MIN-167 staging acceptance remediation

- Branch: `staging`.
- Scope: repair the highest-priority A/D findings from the 2026-05-01 staging A-G acceptance report.
- Local code changes:
  - Added shared metadata builder in `src/lib/metadata.ts`.
  - Added Next `proxy.ts` to pass pathname into the root layout and set `html lang` to `en` on `/en` routes.
  - Completed canonical, OG metadata, Twitter metadata, and `x-default` hreflang for homepage, English homepage, Blog, and Golden Crucible pages.
  - Added `src/app/sitemap.ts`.
  - Changed the homepage contact block to a semantic `footer`.
  - Added `public/apple-touch-icon.png`.
- Verification:
  - `pnpm build`: PASS.
  - Local production preview on `http://127.0.0.1:3001`: PASS for `/`, `/en`, `/blog`, `/golden-crucible`, `/en/golden-crucible`, `/sitemap.xml`, and `/apple-touch-icon.png`.
  - `agent-browser open http://127.0.0.1:3001/en`: PASS.
  - `agent-browser errors`: no browser errors reported.

## Remaining work

- Commit and push after confirmation, then wait for Railway staging deployment.
- Rerun A/D online checks against staging after deployment.
- WP REST write checks B3 and F1-F3 still require `WP_USER` and `WP_APP_PASSWORD`.
- Performance checks C1/C2 still require Lighthouse CLI or a reliable PSI run.
- Runtime font fetch warning and HSTS warning still need separate follow-up.
