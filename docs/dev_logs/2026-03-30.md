# 2026-03-30

## 会话概览

- 当前分支：`codex/cyd-stumpel-home-exploration`
- 本次目标：先验证 Railway 模板创建的 `WordPress` 服务是否可作为新的 Homepage CMS 主候选；若可用，再推进最小安全安装与后续 CMS 主线准备
- 本次结果：
  - 模板 `WordPress` 服务已验证可用，并确认应作为新的 CMS 主候选
  - WordPress 已完成首次安装
  - 基础安全常量已补入 `WordPress` 服务
  - 仓库内已落下 `mindhikers-cms-core` 骨架与 Homepage CMS 读取层
  - 后台管理员密码恢复未完成

## 已确认的实际情况

### 1. Railway 模板 WordPress 可用

已实际验证：

1. `https://wordpress-production-1486.up.railway.app/`
2. `https://wordpress-production-1486.up.railway.app/wp-login.php`
3. `https://wordpress-production-1486.up.railway.app/wp-admin`

验证事实：

1. 三个入口最初都能进入 `wp-admin/install.php`
2. 页面标题为 `WordPress › Installation`
3. 可见语言选择与继续按钮
4. 该服务不再表现为旧 `Primary` 的 `403/404`
5. 也不再表现为 `mindhikers-cms-v2` 的 `502/CRASHED`

### 2. WordPress 已完成首次安装

安装后确认到的事实：

1. 站点标题已变为 `Mindhikers Homepage CMS`
2. `/wp-login.php` 当前可返回正常登录页
3. 登录页语言已为简体中文

### 3. 基础安全常量已写入 WordPress 服务

当前 `WORDPRESS_CONFIG_EXTRA` 已包含：

1. `FORCE_SSL_ADMIN`
2. `DISALLOW_FILE_EDIT`
3. `AUTOMATIC_UPDATER_DISABLED`
4. `WP_POST_REVISIONS`
5. `EMPTY_TRASH_DAYS`

### 4. 当前后台接管仍未打通

已确认到的事实：

1. 安装时密码字段未被自动化工具稳定写入
2. 因此 `mindhikers_admin` 的预期密码不可用
3. `忘记密码` 流程会报“邮件未能发送”
4. 当前 SMTP 未配置

## 本次已落盘的仓库改动

### 1. 新增方案文档

- `Plan/2026-03-30_Mindhikers_WordPress_Template_CMS_Implementation_Plan.md`

### 2. 新增 WordPress MU Plugin 骨架

- `wordpress/mu-plugins/mindhikers-cms-core.php`
- `wordpress/mu-plugins/mindhikers-cms-core/bootstrap.php`
- `wordpress/mu-plugins/mindhikers-cms-core/README.md`

### 3. 新增 Homepage CMS 读取层

- `src/lib/cms/homepage.ts`

### 4. 接入 Homepage CMS 读取层的页面

- `src/app/page.tsx`
- `src/app/en/page.tsx`

### 5. 新增运维恢复脚本

- `ops/wordpress/reset-admin-password.mjs`
- `ops/wordpress/README.md`

## 本次尝试但未完成的事项

### 1. 通过外部数据库连接重置管理员密码

尝试内容：

1. 在隔离的 `/tmp` Node 环境中临时安装 `mysql2`
2. 通过 `MariaDB-e3je` 的公网连接信息执行改密脚本

结果：

1. 临时客户端安装成功
2. 连接 `hopper.proxy.rlwy.net` 时出现连接被关闭或 DNS 不稳定
3. 改密未完成

### 2. 通过 `WORDPRESS_CONFIG_EXTRA` 注入一次性改密逻辑

尝试内容：

1. 通过 Railway 变量注入一次性 token 触发的改密逻辑

结果：

1. 首版代码曾导致站点返回 500
2. 已在同窗口内立即回滚
3. 当前站点已恢复到正常可访问状态
4. 后续又尝试过更简单版本，但未完成最终触发验证

## 当前停点

本窗口结束时的真实停点是：

1. `WordPress` 模板服务是新的 CMS 主候选，这一点已确认
2. WordPress 安装已完成
3. 基础安全常量已写入
4. `mindhikers_admin` 密码仍未恢复到可控状态
5. `mindhikers-cms-core` 尚未部署进 WordPress 的 `mu-plugins`
6. Homepage CMS API 还未进入后台联调阶段

## 下一窗口最合理的顺序

1. 先验证 `https://wordpress-production-1486.up.railway.app/wp-login.php` 仍返回正常登录页
2. 优先恢复 `mindhikers_admin` 密码
3. 登录打通后，再部署 `mindhikers-cms-core`
4. 再联调 Homepage 结构化 API
5. 最后才考虑 `homepage-manage.mindhikers.com` 与 Cloudflare Access
