# 2026-04-03

## 会话概览

- 当前分支：`codex/cyd-stumpel-home-exploration`
- 本次目标：按 `MIN-110` 进入 CMS 灌装实操，并先修复已确认的 `zh` 文案乱码

## 本轮完成

### 1. 进入 CMS 后台并完成账号链路验证

1. 已通过 Cloudflare Access 邮箱验证码登录链路
2. 已使用 WordPress 管理员账号登录后台
3. 已进入 `Mindhikers Homepages` 列表并定位 `Homepage zh` 已发布条目

### 2. 完成 `zh` 乱码字段修复并保存

1. 编辑条目：`Homepage zh`（`post=7`）
2. 修复字段：`about.paragraphs[1]`
3. 修复内容：
   - 从：`入口像���目单`
   - 改为：`入口像节目单`
4. 已点击 `更新` 保存

### 3. 接口回读验收通过

验证命令：

```bash
curl -s https://homepage-manage.mindhikers.com/wp-json/mindhikers/v1/homepage/zh | jq -r '.about.paragraphs[1]'
curl -s https://homepage-manage.mindhikers.com/wp-json/mindhikers/v1/homepage/zh | rg '���|�' -n
curl -s https://homepage-manage.mindhikers.com/wp-json/mindhikers/v1/homepage/en | rg '���|�' -n
```

结果：

1. `zh.about.paragraphs[1]` 返回值已是“入口像节目单…”
2. `zh` / `en` 接口中均未检出 `�` 或 `���`

## 当前状态

1. `MIN-110` 已进入真实灌装执行阶段
2. 当前“已确认乱码问题”已修复并验收
3. 下一步是继续做全字段运营稿核对与补齐（`zh/en`）

## 待继续

1. 按清单逐段核对 `zh/en` 全字段是否为最终运营稿
2. 确认前台 `WORDPRESS_API_URL` 切换窗口
3. 执行正式发布 smoke 与回滚演练

## 14:31 新增：按本地基线全量回灌 CMS（zh / en）

### 1. 执行动作

1. 用户确认采用“本地老版本样式”直接回灌方案
2. 进入 WordPress `Mindhikers Homepages` 后台编辑页：
   - `Homepage zh`（`post=7`）
   - `Homepage en`（`post=9`）
3. 将以下本地 seed 全量覆盖到 CMS Payload 并点击 `更新`：
   - `ops/wordpress/homepage-seeds/homepage-zh.json`
   - `ops/wordpress/homepage-seeds/homepage-en.json`

### 2. 验收结果

1. `wp-json` 回读关键字段符合本地基线：
   - `zh.hero.title` / `zh.about.paragraphs[1]` / `zh.product.featured.title`
   - `en.hero.title` / `en.about.paragraphs[1]` / `en.product.featured.title`
2. `zh` / `en` 接口均未检出乱码字符（`�` / `���`）
3. 中文问题字段保持修复后状态：
   - `about.paragraphs[1]` 中“入口像节目单”已稳定存在

### 3. 本轮结论

1. CMS 内容侧已恢复到本地 seed 对齐状态（`zh/en`）
2. 下一步不再是“内容修复”，而是“前台域名挂载完成后的读取验收与发布 smoke”
3. 按用户要求，当前不再展开域名挂载检查，待域名挂载完成后再做前台验收

## 15:12 新增：前台 `www` 域名切到 Homepage Railway 服务

### 1. 进度读取与平台现状核对

1. 已读取：
   - `docs/dev_logs/HANDOFF.md`
   - `docs/rules.md`
   - `docs/plans/2026-04-02_MIN-110_CMS_Operate_And_Homepage_Domain_Release_Plan.md`
2. Railway 项目核对结果：
   - Project：`Mindhikers-Homepage`
   - Workspace：`Zhou Lu's Projects`
   - Homepage 服务：`Mindhikers-Homepage`
   - Homepage Railway 服务域名：`mindhikers-homepage-production.up.railway.app`
   - Homepage 自定义域名：`www.mindhikers.com`
3. Cloudflare / 外部探针起始状态：
   - `www.mindhikers.com` 仍指向旧 Vercel CNAME，访问返回 `DEPLOYMENT_NOT_FOUND`
   - `mindhikers.com` 根域仍 302 到 `https://www.spaceship.com`
   - `homepage-manage.mindhikers.com/wp-json/mindhikers/v1/homepage/zh` 返回 200

### 2. 本轮执行动作

1. 已将 Cloudflare 中 `www.mindhikers.com` 的 DNS 从旧 Vercel 目标切到：
   - `mindhikers-homepage-production.up.railway.app`
2. 首次切换为 `proxied=true` 时，外部访问进入 Railway 边缘，但返回：
   - `Application not found`
3. 为排除 Railway 自定义域名验证未完成问题，已将 `www.mindhikers.com` 改为：
   - `CNAME -> mindhikers-homepage-production.up.railway.app`
   - `proxied=false`

### 3. 当前验收结果

1. `mindhikers-homepage-production.up.railway.app` 自身返回 200，说明 Homepage 服务本身正常
2. `http://www.mindhikers.com/` 与 `http://www.mindhikers.com/en` 已返回 301 到对应 `https://www.mindhikers.com/...`
3. `https://www.mindhikers.com/` 与 `/en` 当前不再是旧 Vercel 404，而是进入 Railway 证书阶段
4. 当前 HTTPS 失败口径为：
   - `SSL: no alternative certificate subject name matches target host name 'www.mindhikers.com'`
5. 这说明：
   - `www` 的 DNS 归属已切到 Homepage
   - 当前剩余阻塞点是 Railway 对 `www.mindhikers.com` 的证书签发 / 激活尚未完成

### 4. 仍未完成项

1. `https://www.mindhikers.com/` 的证书尚未对齐，暂时无法做最终公开页 smoke
2. `mindhikers.com` 根域仍然 302 到 `https://www.spaceship.com`，尚未收口到 `www`
3. 前台 `WORDPRESS_API_URL` 正式读取切换与 smoke 仍未开始

### 5. 下一窗口直接做

1. 继续轮询 `https://www.mindhikers.com/` 与 `/en`，直到 Railway 证书对齐
2. 证书生效后立刻执行：
   - `/` 与 `/en` 页面读取验收
   - `homepage-manage` 的 `wp-json` 对照验收
   - revalidate 与 smoke 清单
3. 单独处理根域 `mindhikers.com -> https://www.mindhikers.com` 收口，不与本轮 `www` 接入混做

## 15:26 新增：根域 `mindhikers.com` 已正式挂到 Homepage

### 1. 本轮完成

1. 已在 Railway 为 `Mindhikers-Homepage` 新增 apex custom domain：
   - `mindhikers.com`
2. Railway 返回的根域指向要求已落到 Cloudflare：
   - `mindhikers.com -> rltupsq3.up.railway.app`（CNAME, proxied=true）
3. 已补充 Railway ownership 验证记录：
   - `_railway-verify.mindhikers.com` TXT
4. 经过等待验证后，根域公开读取通过：
   - `https://mindhikers.com/` -> 200
   - `https://mindhikers.com/en` -> 200
   - `https://mindhikers.com/blog` -> 200

### 2. 当前剩余问题

1. `www.mindhikers.com` 仍返回 Railway fallback 404，尚未与当前正式入口统一
2. 页面 HTML 中仍有 `www.mindhikers.com` 相关 canonical / OG URL 口径，后续应统一到最终正式域名

### 3. 9 个服务的实际使用归类（第一版）

#### 仍在实际链路中

1. `Mindhikers-Homepage`
   - 当前承载 `mindhikers.com`
2. `WordPress-L1ta`
   - 当前承载 `homepage-manage.mindhikers.com`
3. `MariaDB-94P8`
   - 当前作为 `WordPress-L1ta` 对应数据库保留

#### 旧现场 / 待删候选

1. `Primary`
   - 历史域名：`cms.mindhikers.com`、`manage.mindhikers.com`
   - 当前外部探针均跳 `https://www.spaceship.com`
2. `MariaDB`
   - 视为 `Primary` 的旧数据库候选
3. `WordPress`
   - 无当前正式自定义域名，视为旧实例
4. `MySQL`
   - 视为旧 `WordPress` 数据库候选
5. `mindhikers-cms-v2`
   - 已 crashed，多日未进入正式链路
6. `MariaDB-e3je`
   - 视为 `mindhikers-cms-v2` 数据库候选

### 4. 删除前提醒

1. 上述 6 个候选服务一旦删除，会连带清理对应 deployments / volumes / 历史数据
2. 建议先完成 `www` 收口与前台 smoke，再执行最终删除

## 17:27 新增：Homepage 域名正式上线并完成生产资源收敛

### 1. 域名最终口径

1. 正式首页域名已确定为：
   - `https://mindhikers.com`
2. `www` 已完成收口：
   - 通过 Cloudflare Redirect Rule 将 `https://www.mindhikers.com/*`
   - 301 跳转到 `https://mindhikers.com/${1}`
3. CMS 管理域名保持不变：
   - `https://homepage-manage.mindhikers.com`

### 2. 本轮实际执行结果

1. 用户在 Cloudflare 手工完成了 `www -> apex` 的 Redirect Rule 部署
2. 用户确认 `https://www.mindhikers.com/` 已成功跳转
3. 用户在 Railway 面板中手工删除了 6 个旧服务：
   - `mindhikers-cms-v2`
   - `MariaDB-e3je`
   - `WordPress`
   - `MySQL`
   - `Primary`
   - `MariaDB`
4. 用户继续在 Railway 面板中手工删除了 6 个孤儿 volume：
   - `primary-volume`
   - `mindhikers-cms-v2-volume`
   - `wordpress-volume`
   - `mysql-volume`
   - `mariadb-volume`
   - `mariadb-volume-8sLv`

### 3. 当前保留的生产资源（最终版）

#### 保留服务

1. `Mindhikers-Homepage`
   - 承载正式首页域名 `mindhikers.com`
2. `WordPress-L1ta`
   - 承载 CMS 管理域名 `homepage-manage.mindhikers.com`
3. `MariaDB-94P8`
   - 当前 CMS 生产数据库

#### 保留 volume

1. `mariadb-volume-x1on`
2. `wordpress-volume-vRzA`

### 4. 本轮结论

1. “把 Railway 的 Homepage 服务挂到 mindhikers 正式域名”这件事已完成
2. Railway 项目已从历史 9 服务混乱现场，收敛为 3 个生产服务 + 2 个生产 volume
3. 下一个窗口不需要再重复做域名归属排障，应该直接基于已上线事实继续做 smoke / revalidate / 口径统一

### 5. 下一窗口建议

1. 公开页 smoke：
   - `mindhikers.com/`
   - `mindhikers.com/en`
   - `mindhikers.com/blog`
2. CMS 接口对照：
   - `homepage-manage.mindhikers.com/wp-json/mindhikers/v1/homepage/zh`
   - `homepage-manage.mindhikers.com/wp-json/mindhikers/v1/homepage/en`
3. 口径统一复核：
   - canonical
   - OG URL
   - sitemap / robots / revalidate 路径
